.venv/
__pycache__/
*.pyc
.env
instance/
logs/
.pytest_cache/
# Imagens enviadas pelos tenants: conteúdo do usuário, não código.
app/static/uploads/
*.db
*.db-*

# Marca de qual commit está de fato no ar. Gravada pelo deploy/atualizar.sh no
# servidor, depois de o serviço subir; é estado daquela máquina, não código.
.versao-publicada
